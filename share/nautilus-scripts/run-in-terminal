#!/bin/bash

# wykonaj skrypt w terminalu

# "Hold" jest profilem, w którym terminal nie jest zamykany po zakończeniu polecenia
gnome-terminal --profile=Hold -e "${NAUTILUS_SCRIPT_SELECTED_FILE_PATHS%?}"

#~ xterm -hold -e "${NAUTILUS_SCRIPT_SELECTED_FILE_PATHS%?}"
