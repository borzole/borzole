#!/bin/bash

# silnik eksport
# by borzole.one.pl

# Cokolwiek eksperymentuje, robię to na skryptach "silnik" oraz "robot".
# Gdy uznam, że skrypt można skończyć, albo odłożyć na później, wówczas:
# Alt+F2	
#		silnik-ex nowa_nazwa
# i tyle, można zająć się innym skryptem

cp -p /usr/local/sbin/silnik /usr/local/sbin/"$1"
chmod +x "$1"
