#!/bin/bash

DATA=$(date +%Y.%m.%d-%H:%M)
SYSTEM=$(cat /etc/system-release | sed -e 's/release\ *//g')
LOG="$HOME/zainstalowane -- ${SYSTEM} $(arch) -- ${DATA}.log"

rpm -qa --qf "%{name} \n" | sort -u > "$LOG"
