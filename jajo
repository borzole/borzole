#!/bin/bash

# http://forum.ubuntu.pl/showthread.php?t=45758&page=4

_czas(){ 
		sleep $sec 
		zenity --info --name "Gotowe!" --text "Jajo gotowe, \nżyczę smacznego" 
} 

min=$(zenity --scale --text "Ile minut gotujemy jajo?") 

if [ "$?" = "1" ] ; then 
	exit 0 
else 
	sec=$[$min*60] 
	_czas
fi
